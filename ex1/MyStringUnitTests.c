#include "MyString.h"


bool filterRemoveB(const char * ch)
{
    if (ch != NULL && *ch == 'b')
    {
        return true;
    }
    return false;
}

int comp(const char c1, const char c2)
{
    return 0;
}


